// Math é uma biblioteca matemática do JS
    // (X) 'const math = new Math()'  Math é uma biblioteca de classe estática, e por isso não precisa ser instanciada!

const mat = document.getElementById("mat")

//mat.innerHTML=Math.random()                       //Gera um número aleatório entre 0 e 1
//mat.innerHTML=Math.random()*10                    //Gera um número aleatório entre 0 e 10
//mat.innerHTML=Math.random()*100                   //Gera um número aleatório entre 0 e 100
//mat.innerHTML=Math.floor(Math.random()*10)        //Gera apenas a parte inteira do número aleatório (entre 0 e 9)

//const num=Math.random()*10
//mat.innerHTML=Math.round(num)                     //Gera apenas a parte inteira do número aleatório (entre 0 e 10)

//mat.innerHTML=Math.sqrt(64)                       //Retorna a raíz quadrada do número passado no parâmetro
mat.innerHTML=Math.pow(10,2)                        //Retorna 10 elevado a 2

